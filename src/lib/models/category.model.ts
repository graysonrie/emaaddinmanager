export interface CategoryModel {
  /** Will be a directory name if the addin registry is local */
  name: string;
  /** Will be a directory path if the addin registry is local */
  fullPath: string;
}
